import * as React from 'react';
import { Text, View } from 'react-native';
import styles from '../../constants/styles';

export default function settingCli() {
    return (
      <View style={styles.container}>
        <Text>Setting screen</Text>
      </View>
    );
  }